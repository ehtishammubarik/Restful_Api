name: drone-test
kind: pipeline
type: docker

steps:
- name: test
  image: dind
  volumes:
  - name: docker_sock
    path: /var/run/docker.sock
  commands:
  - up
volumes:
  - name: docker_sock
    host:
      path: /var/run/docker.sock
